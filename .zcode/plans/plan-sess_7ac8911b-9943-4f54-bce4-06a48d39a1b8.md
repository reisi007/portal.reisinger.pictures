## Plan: AGENTS.todo.md aktualisieren (nur Doku, kein Code)

### Was passiert
Die `AGENTS.todo.md` wird um einen präzisen, in einem neuen Chat abarbeitbaren TODO-Block für die SRP-Stilllegung erweitert. Keine Code-Änderungen.

### Neue Section: "SRP-Stilllegung — Vorbereitet für neuen Chat (2026-07-14)"

Inhalt (basiert auf der vollständigen Inventur):

**Kontext-Klarstellung:**
- Refactor `4075ad8` hat die DB-basierte Brand-Infrastruktur geschaffen. SRP ist als Beispiel-Brand in der `brands`-Tabelle (V027-Seed) **erhalten geblieben** — die Frage des Users wird mit "Ja" beantwortet.
- Frontend ist bereits `isSrp`-frei (nutzt `useBrandConfig`-API).
- Was fehlt: Backend-`isSrp`-Branches + Frontend-Typen-Vereinfachung.

**Strategie (User-Entscheidung 14.07.2026):**
- `Brand::SRP`-Enum-Case **bleibt erhalten** (SRP als Beispiel-Brand in DB).
- Production-Branches (Mail/PDF/Invoice `isSrp`) werden auf DB-Config umgestellt oder kollabiert.
- Frontend-Typen vereinfacht: `'rp' | 'srp' | null` → `'rp' | null` im User-Management.

**TODO-Checkliste (kategorisiert, mit file:line):**

1. **Backend Production Branches (9 Stellen, 6 Dateien)** — `isSrp`/`=== 'srp'` kollabieren:
   - `BrandRegistry.php:24-26` (host-Fallback), `:61-64` (`isSrp()`-Methode)
   - `SettingResolver.php:18-21` (`isSrp()`-Wrapper)
   - `AbstractBrandAwareMailable.php:29,43,50` (URL/BCC/From)
   - `InvoiceController.php:35,73,83`, `ContractPdfService.php:56`, `InvoiceMail.php:44` (`isSrp`-Flag)
   - `UpdateUserRequest.php:28` (Validation `in:rp,srp`)

2. **PDF Blade Views (4 Dateien)** — `isSrp`-Ternaries auflösen:
   - `pdf/header.blade.php:8-9`, `pdf/invoice.blade.php:7-8,28`, `pdf/manual_offer.blade.php:7-8,27`, `pdf/contract_signatures.blade.php:33`

3. **Frontend Typen vereinfachen (3 Dateien)** — `'rp' | 'srp'` → `'rp'`:
   - `UserPermissionsModal.tsx:13,33,42,128,132` (inkl. `<option value="srp">` entfernen)
   - `ManagementUserView.tsx:33`
   - `useUsers.ts:28,54`

4. **Frontend brandRegistry + index.html** — `getBrandFromHostname` und Boot-Script: `srp`-Branches entfernen oder als Alias auf `rp` belassen.

5. **Tests (23 Backend + 8 Frontend Files, 226+33 Matches)** — das ist der größte Block:
   - SRP-only Test-Files löschen: `SrpSettingsSeederTest.php`, `CheckoutServiceSrpTest.php`, `srp-dashboard.spec.ts`, `srp-volume-pricing.spec.ts`
   - Übrige Test-Files: `Brand::SRP`/`'srp'`-Referenzen durch `Brand::B2B`/`'rp'` ersetzen ODER Tests umschreiben, falls sie SRP-spezifisches Verhalten prüfen.

6. **Kosmetik:** Stale Comments in `CouponInput.tsx:8`, `UpdateUserRequest.php:26,56`, `UserController.php:159`, `SettingResolver.php:11-13`.

7. **Optional Clarity:** `ShootingCalculatorModal.tsx` calcMode `'srp'`→`'flex'`, `VolumeLicensingStrategy.php` tier `'srp'`→`'volume'` (NOT load-bearing, pricing-mode labels).

**Offen bleiben:**
- Per-Gallery Licensing Override (separater PR)
- `features/`-Dokumentation der neuen Setting-basierten Pricing-Architektur
- Playwright @smoke neu validieren nach SRP-Cleanup

### Was NICHT passiert
- Keine Code-Änderungen.
- Keine Migration.
- Kein Test-Cleanup.
- Nur `AGENTS.todo.md` wird editiert.