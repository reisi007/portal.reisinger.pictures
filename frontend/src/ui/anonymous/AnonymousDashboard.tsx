import React from 'react';
import SearchView from '../SearchView';

export default function AnonymousDashboard() {
    // Kapselt die Gäste-Ansicht (Neueste Entdeckungen / Suche) 
    // mit dem sauberen Rollen-Präfix "Anonymous".
    return <SearchView />;
}
