services:
  search:
    image: getmeili/meilisearch:v1.48.3
    container_name: portal_search_test
    restart: unless-stopped
    environment:
      MEILI_ENV: development
      MEILI_MASTER_KEY: test_meili_secret
    ports: ["7701:7700"]
    volumes: ["search_data_test:/meili_data"]

volumes:
  search_data_test:
